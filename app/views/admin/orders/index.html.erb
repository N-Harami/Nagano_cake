<h2>注文履歴一覧</h2>
<table>
  <tbody>
    <tr>
      <th>購入日時</th>
      <th>購入者</th>
      <th>注文個数</th>
      <th>注文ステータス</th>
    </tr>
  </tbody>
  <% @orders.each do |order| %>
    <tbody>
      <tr>
        <th><%= order.created_at %></th>
        <th>
          <% link_to admin_order_path(ordr) do %>
            <%= order.customers.last_name %><%= order.customers.first_name %>
          <% end %>
        </th>
        <th><%= order.items.count %></th>
        <th><%= order.oder_details.production_status %></th>
      </tr>
    <tbody>
  <% end %>
</table>